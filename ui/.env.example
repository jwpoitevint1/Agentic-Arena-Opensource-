# Server-side proxy configuration.
BACKEND_URL=https://api.example.com

# Optional server-side authentication. Never expose these values through client-side variables.
# BACKEND_AUTH_HEADER=x-arena-api-key
# BACKEND_AUTH_VALUE=replace-with-managed-secret
